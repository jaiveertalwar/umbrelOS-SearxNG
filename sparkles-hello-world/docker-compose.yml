version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: searxng
      APP_PORT: 8080

  searxng:
    image: searxng/searxng:latest
    restart: on-failure
    stop_grace_period: 1m
    environment:
      BASE_URL: http://localhost:8080/
    ports:
      - "8080:8080"
